import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "New Product | G-MART Admin",
};

export default function NewProductLayout({ children }: { children: React.ReactNode }) {
  return children;
}
